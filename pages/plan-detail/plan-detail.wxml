<!--pages/plan-detail/plan-detail.wxml 计划详情-->
<navigation-bar title="训练详情" color="#1A1A1A" background="#FFFFFF"></navigation-bar>
<px-toast></px-toast>
<scroll-view class="scrollarea" scroll-y type="list" wx:if="{{plan}}">
  <view class="page">

    <!-- 计划信息 -->
    <view class="head g-card">
      <view class="name">{{plan.name}}</view>
      <view class="meta sub">{{plan.duration}} 分钟 · 约 {{plan.calories}} kcal · {{plan.exTotal}} 个动作</view>
      <view class="summary">{{plan.summary}}</view>
    </view>

    <!-- 训练提示 -->
    <view class="notice">
      <view class="notice-title">训练提示</view>
      <view class="notice-text">{{plan.notice}}</view>
      <view class="notice-round">{{plan.roundsText}}</view>
    </view>

    <!-- 动作清单 -->
    <view class="ex-head">
      <view class="flex1">训练动作</view>
      <text class="weak">点击动作查看教学</text>
    </view>
    <view class="adjust-tip" wx:if="{{hasAdjustments}}">
      <text>已使用个人调整</text>
      <text wx:if="{{!readonly}}" class="adjust-reset" hover-class="hover" hover-stay-time="100" bindtap="onRestoreDefault">恢复默认</text>
    </view>

    <view class="g-card ex-list">
      <view
        class="ex-item"
        wx:for="{{items}}"
        wx:key="actionId"
        hover-class="hover" hover-stay-time="100"
        data-id="{{item.actionId}}"
        bindtap="goAction"
      >
        <view class="ex-idx">{{index + 1}}</view>
        <view class="flex1">
          <view class="ex-name">{{item.name}}</view>
          <view class="ex-meta weak">{{item.sets}} 组 × {{item.reps}}<text wx:if="{{item.rest}}"> · {{item.rest}}</text></view>
          <view class="set-adjust" wx:if="{{!readonly}}" catchtap="onAdjustArea">
            <text class="set-label">组数</text>
            <text class="set-control {{item.canReduce ? '' : 'disabled'}}" data-id="{{item.actionId}}" data-delta="-1" catchtap="onAdjustSet">−</text>
            <text class="set-value">{{item.sets}}</text>
            <text class="set-control {{item.canIncrease ? '' : 'disabled'}}" data-id="{{item.actionId}}" data-delta="1" catchtap="onAdjustSet">＋</text>
          </view>
        </view>
        <text class="ex-arrow">›</text>
      </view>
    </view>

  </view>
</scroll-view>

<!-- 底部操作栏：只读模式（计划库浏览）不提供开始训练入口 -->
<view class="bar" wx:if="{{plan && !readonly}}">
  <view class="bar-info">
    <view class="bar-time num">{{plan.duration}} 分钟</view>
    <view class="bar-kcal weak">约 {{plan.calories}} kcal</view>
  </view>
  <view class="btn btn-primary bar-btn bar-main" hover-class="hover" hover-stay-time="100" bindtap="onWorkout">{{hasActiveSession ? '继续训练' : '开始训练'}}</view>
</view>

<!-- 切换计划确认弹层 -->
<view wx:if="{{showSwitchConfirm}}" class="switch-mask" catchtouchmove="noop">
  <view class="switch-dialog" catchtap="noop">
    <view class="switch-title">有未完成训练</view>
    <view class="switch-copy">是否放弃「{{switchActiveName}}」的进度</view>
    <view class="switch-btns">
      <view class="switch-btn cancel" hover-class="hover" hover-stay-time="100" bindtap="onCancelSwitch">取消</view>
      <view class="switch-btn primary" hover-class="hover" hover-stay-time="100" bindtap="onConfirmSwitch">放弃并开始</view>
    </view>
  </view>
</view>

<!-- 登录引导弹层：页内像素弹窗 -->
<view wx:if="{{showLoginDialog}}" class="px-mask" bindtap="onCancelLogin" catchtouchmove="noop">
  <view class="px-dialog" catchtap="noop">
    <view class="px-dialog-title">请先登录</view>
    <view class="px-dialog-copy">登录后，便于云端数据同步</view>
    <view class="px-dialog-btns">
      <view class="px-dialog-btn cancel" hover-class="hover" hover-stay-time="100" bindtap="onCancelLogin">暂不</view>
      <view class="px-dialog-btn danger" hover-class="hover" hover-stay-time="100" bindtap="onConfirmLogin">去登录</view>
    </view>
  </view>
</view>
